<body class="w-full h-full bg-gradient-to-b from-black via-gray-900 to-black text-white font-sans overflow-x-hidden">

  <!-- Leadership Hierarchy Section -->
  <section class="py-24 px-4 md:px-10 lg:px-20">
    <h2 class="text-4xl lg:text-5xl font-extrabold text-center text-blue-400 mb-20 glow">
      Leadership Hierarchy – Orgatro WorkHub
    </h2>

    <!-- CEO -->
    <div class="flex justify-center mb-28 animate-slide-up-slow">
      <div class="profile-card">
        <img src="https://i.ibb.co/ks2fq8vv/3.png" alt="CK SONJOY - CEO" class="rounded-full w-32 h-32 mx-auto mt-6 mb-4 border-4 border-blue-400 shadow-xl shimmer">
        <h3 class="text-2xl font-bold text-blue-300">CK SONJOY (CEO)</h3>
        <p class="text-gray-400">Chief Executive Officer</p>
        <p class="text-sm text-gray-300 px-4 pb-4">
          Vision-focused executive overseeing IT strategy, innovation, infrastructure investment, and organizational alignment with business goals.
        </p>
        <a href="https://www.facebook.com/share/1CTbtV41ow/" class="animated-link pb-6 block">Facebook</a>
      </div>
    </div>

    <!-- Co-Founder & CIO -->
    <div class="grid md:grid-cols-2 gap-10 justify-center items-start mb-28">
      <!-- Co-Founder -->
      <div class="flex flex-col items-center animate-slide-up-slow">
        <div class="profile-card">
          <img src="https://i.ibb.co/hxxpHM8t/1.png" alt="Nihal Roy - Co-Founder" class="rounded-full w-28 h-28 mx-auto mt-6 mb-4 border-4 border-blue-400 shadow-md shimmer">
          <h3 class="text-xl font-bold text-blue-300">Nihal Roy (CO)</h3>
          <p class="text-gray-400">Co-Founder</p>
          <p class="text-sm text-gray-300 px-4 pb-4">
            Operational leader ensuring seamless IT service delivery, efficient workflow management, and continuous process optimization across teams.
          </p>
          <a href="https://www.facebook.com/share/173CbWsLWK/" class="animated-link pb-6 block">Facebook</a>
        </div>
      </div>

      <!-- CIO -->
      <div class="flex flex-col items-center animate-slide-up-slow">
        <div class="profile-card">
          <img src="https://i.ibb.co/DDySj2JX/2.png" alt="Heaven Halder - CIO" class="rounded-full w-28 h-28 mx-auto mt-6 mb-4 border-4 border-blue-400 shadow-md shimmer">
          <h3 class="text-xl font-bold text-blue-300">Heaven Halder (CIO)</h3>
          <p class="text-gray-400">Chief Information Officer</p>
          <p class="text-sm text-gray-300 px-4 pb-4">
            Chief technology officer directing IT infrastructure, applications, data governance, cybersecurity, and digital transformation initiatives.
          </p>
          <a href="https://www.facebook.com/share/1EzXR9hvZk/" class="animated-link pb-6 block">Facebook</a>
        </div>
      </div>
    </div>

    <!-- CMO -->
    <div class="flex justify-center mb-28 animate-slide-up-slow">
      <div class="profile-card">
        <img src="https://i.ibb.co/HfMFkrc7/4.png" alt="ARTTIK DIPU - CMO" class="rounded-full w-28 h-28 mx-auto mt-6 mb-4 border-4 border-blue-400 shadow-md shimmer">
        <h3 class="text-xl font-bold text-blue-300">Arttik Dipu (CMO)</h3>
        <p class="text-gray-400">Chief Marketing Officer</p>
        <p class="text-sm text-gray-300 px-4 pb-4">
          Marketing executive crafting Orgatro IT’s brand messaging, digital outreach, client engagement strategy, and stakeholder communications.
        </p>
        <a href="https://www.facebook.com/share/1DnXsp9L6g/" class="animated-link pb-6 block">Facebook</a>
      </div>
    </div>

    <!-- Managing Director -->
    <div class="flex justify-center animate-slide-up-slow">
      <div class="profile-card">
        <img src="https://i.ibb.co/nqrV9kw4/5.png" alt="Sabbir Ahmed - Managing Director" class="rounded-full w-28 h-28 mx-auto mt-6 mb-4 border-4 border-blue-400 shadow-md shimmer">
        <h3 class="text-xl font-bold text-blue-300">Sabbir Ahmed (MD)</h3>
        <p class="text-gray-400">Managing Director</p>
        <p class="text-sm text-gray-300 px-4 pb-4">
          Driving organizational growth via client partnerships, performance metrics, and cross-functional leadership in IT services.
        </p>
        <a href="https://www.facebook.com/share/1A7oxpbGLA/" class="animated-link pb-6 block">Facebook</a>
      </div>
    </div>
  </section>

  <!-- Animations & Styling -->
  <style>
    @keyframes fade-in-down {
      0% { opacity: 0; transform: translateY(-30px); }
      100% { opacity: 1; transform: translateY(0); }
    }
    @keyframes slide-up-slow {
      0% { opacity: 0; transform: translateY(60px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    .animate-fade-in-down {
      animation: fade-in-down 1s ease-out forwards;
    }
    .animate-slide-up-slow {
      animation: slide-up-slow 1.2s ease-out forwards;
    }

    .profile-card {
      background: linear-gradient(145deg, #0f172a, #1e293b);
      border: .0625rem solid #3b82f6;
      border-radius: 1.5rem;
      width: 100%;
      max-width: 340px;
      text-align: center;
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      box-shadow: 0 .625rem 1.875rem rgba(0, 255, 255, 0.15);
    }

    .profile-card:hover {
      transform: scale(1.04);
      box-shadow: 0 .75rem 2.1875rem rgba(0, 255, 255, 0.3);
    }

    .animated-link {
      color: #60a5fa;
      text-decoration: underline;
      transition: color 0.3s ease, text-shadow 0.3s ease;
    }

    .animated-link:hover {
      color: #93c5fd;
      text-shadow: 0 0 .3125rem rgba(96, 165, 250, 0.8);
    }

    .glow {
      text-shadow: 0 0 .625rem rgba(96, 165, 250, 0.6);
    }

    .shimmer {
      animation: shimmer 2.5s infinite;
      background: linear-gradient(90deg, rgba(0, 0, 0, 0) 0%, rgba(0, 255, 255, 0.2) 50%, rgba(0, 0, 0, 0) 100%);
      background-size: 200% 100%;
    }
    a {
      text-decoration: none;
    }
    @keyframes shimmer {
      0% { background-position: -100% 0; }
      100% { background-position: 100% 0; }
    }
  </style>

  <script>
    // Automatically add animation only once (no conflict)
    document.addEventListener("DOMContentLoaded", () => {
      const animEls = document.querySelectorAll('.animate-fade-in-down, .animate-slide-up-slow');
      animEls.forEach(el => {
        el.style.opacity = 0;
        el.getBoundingClientRect(); // Force reflow
        el.style.opacity = 1;
      });
    });
  </script>

</body>
<script>
  // Add a floating particles background animation
  document.addEventListener("DOMContentLoaded", () => {
    const canvas = document.createElement('canvas');
    canvas.style.position = 'fixed';
    canvas.style.top = 0;
    canvas.style.left = 0;
    canvas.style.width = '100vw';
    canvas.style.height = '100vh';
    canvas.style.zIndex = 0;
    canvas.style.pointerEvents = 'none';
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    document.body.prepend(canvas);

    const ctx = canvas.getContext('2d');
    const particles = [];
    const colors = ['#60a5fa', '#38bdf8', '#818cf8', '#f472b6'];

    function resizeCanvas() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }
    window.addEventListener('resize', resizeCanvas);

    for (let i = 0; i < 36; i++) {
      particles.push({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        r: 1.5 + Math.random() * 2.5,
        d: 0.5 + Math.random() * 1.5,
        color: colors[Math.floor(Math.random() * colors.length)],
        dx: (Math.random() - 0.5) * 0.6,
        dy: (Math.random() - 0.5) * 0.6
      });
    }

    function drawParticles() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      for (const p of particles) {
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.r, 0, 2 * Math.PI);
        ctx.fillStyle = p.color;
        ctx.globalAlpha = 0.7;
        ctx.shadowColor = p.color;
        ctx.shadowBlur = 8;
        ctx.fill();
        ctx.globalAlpha = 1;
        ctx.shadowBlur = 0;
      }
    }

    function updateParticles() {
      for (const p of particles) {
        p.x += p.dx * p.d;
        p.y += p.dy * p.d;
        if (p.x < 0 || p.x > canvas.width) p.dx *= -1;
        if (p.y < 0 || p.y > canvas.height) p.dy *= -1;
      }
    }

    function animate() {
      drawParticles();
      updateParticles();
      requestAnimationFrame(animate);
    }

    animate();
  });
</script>