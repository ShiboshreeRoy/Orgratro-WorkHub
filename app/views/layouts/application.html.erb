<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Orgratro Work Hub" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="orgratro_it.png" type="image/png">
    <link rel="icon" href="<%= asset_path('orgratro_it.svg') %>" type="image/x-icon">  
    <link rel="apple-touch-icon" href="/orgratro_it.png" sizes="192x192">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container">
    <%= render "Shared/Navbar"%>
    <%= render "Shared/Toster" %>
      <%= yield %>
    <%= render "Shared/Footer" %>
    </main>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js" crossorigin="anonymous"></script>
  </body>
</html>
